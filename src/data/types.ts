export type NotificationType = {
  userImageURL: string;
  userName: string;
  time: string;
  activityText: string;
  postName?: string;
  groupName?: string;
  description?: string;
  imageURL?: string;
  isRead: boolean;
};
